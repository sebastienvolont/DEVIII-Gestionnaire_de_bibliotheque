{% extends 'base.html.twig' %}

{% block body %}



    {% if livretab|length  == 0 %} 
    <center><h1> Pas de résultats trouvés pour cette recherche</h1></center>
    {% else %}
     <center><h1> {{livretab|length}} {{(livretab|length>1) ? "résultats" : "résultat"}} </h1></center>
    {% endif %}

    <div class="container"> 

    {% for livre in livretab %}

        {% if livre["nom"] != "Pas de résultat" %}
        <div class="row">
            <div class="col-12 col-md-12 col-lg-12">
        
                <div class="jumbotron">
            <h2>{{livre["nom"]}}</h2>
            <h3>{{livre["auteur"]}}</h3> <br>
            <img src={{livre["img"]}} alt="couverture livre recherché"><br>
            <p>{{livre["genre"]}}</p>
            <form method="POST" action="#">
                <button class="btn btn-primary" type="submit" name="livreChoisi" value="{{loop.index}}">Ajouter ce livre à ma bibliothèque</button></a>
            </form>

                </div>
            </div>
        </div>


        {% endif %}

    {% endfor %}

    </div>


{% endblock %}